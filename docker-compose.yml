services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: freelancer-crm
    restart: unless-stopped
    ports:
      - '3000:3000'
    environment:
      - NODE_ENV=production
      - PORT=3000
      - HOSTNAME=0.0.0.0
      # Uncomment when wiring a database
      # - DATABASE_URL=postgresql://${POSTGRES_USER:-crm}:${POSTGRES_PASSWORD:-crm_secret}@db:5432/${POSTGRES_DB:-crm_db}?schema=public
    healthcheck:
      test: ['CMD', 'wget', '--no-verbose', '--tries=1', '--spider', 'http://127.0.0.1:3000/api/health']
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s

  # Optional PostgreSQL service for unified Coolify Stack deployment
  # db:
  #   image: postgres:16-alpine
  #   container_name: freelancer-crm-db
  #   restart: unless-stopped
  #   environment:
  #     POSTGRES_USER: crm
  #     POSTGRES_PASSWORD: crm_secret
  #     POSTGRES_DB: crm_db
  #   volumes:
  #     - crm_db_data:/var/lib/postgresql/data
  #   healthcheck:
  #     test: ['CMD-SHELL', 'pg_isready -U crm -d crm_db']
  #     interval: 10s
  #     timeout: 5s
  #     retries: 5

# volumes:
#   crm_db_data:
